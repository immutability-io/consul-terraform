{
  "service": {
    "name": "vault",
    "port": 8200,
    "tags": ["vault", "urlprefix-vault.service.consul/"],
    "check": {
      "id": "vault-health",
      "name": "Running on port 8200",
      "script": "VAULT_ADDR=https://vault_url:8200 vault status",
      "interval": "10s",
      "timeout": "1s"
    }
  }
}
